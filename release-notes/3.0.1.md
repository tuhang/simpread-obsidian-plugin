# 3.0.1 修复版

* `Fix` 导入到 Obsidian 时，相同文件名的笔记不再被覆盖。

* `Optimize` 当目标 Markdown 已存在时，自动生成带可读时间戳的新文件名，例如 `Title-2026-06-17_15-30-45.md`。

* `Optimize` 时间戳文件名避免使用 `:`，提升 Windows、macOS 与跨平台同步场景的兼容性。

* `Optimize` 保留已有标注的增量更新行为，避免将标注同步误拆成新的笔记文件。
